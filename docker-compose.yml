services:
  chore-calendar:
    build: .
    ports:
      - "6969:3000"
    volumes:
      # Persist database between container restarts
      - chore-data:/app/data
    restart: unless-stopped

volumes:
  chore-data:
